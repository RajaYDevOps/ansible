- name: varibles preference
  hosts: frontend
  vars:
   full_name: "Siva from play"
  vars_files:
   course.yaml
  vars_prompt:
  - name: full_name
    prompt: "Please enter your fullname"
    private: false
  tasks:
  - name: print variable preference
    vars:
     #full_name: "Siva from task level"
    debug:
      msg: "Hi, {{ full_name }}"


      #var preference
      #1. command level
      #2. task level