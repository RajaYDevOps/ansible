- name: demo on filters
  hosts: local
  connection: local
  vars:
    topics: Linux,shell,ansible,terraform
    tagMap:
      name: ansible
      project: roboshop
      environment: dev
    #full_name: Siva Kumar reddy M
    tasks:
    - name: convert string to list
      debug:
        msg: "Topics are: {{ topics | split(',') }}"
